---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: modelcar-storage
  namespace: modelcar-pipelines
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: gp3-csi  # Adjust based on your cluster's storage class
